# GitHub personal access token with permission to create issues in your project repos
GITHUB_TOKEN=your_github_token_here

# Optional API key. If set, Custom GPT must send this in the x-api-key header.
API_KEY=change_this_to_a_strong_secret

# Local/default port
PORT=3000
